import epackage	"inputs/rdsSimplified.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package rdsSimplified }
	constraints = { package queries }
	solver = ViatraSolver
	scope = {
		#node += 32,
		#<rdsSimplified::Database> += 1..1
	}
	
	config = {
		runtime = 180,
		log-level = normal
	}
	
	runs = 1
	number = 1
	
	debug =			"outputs157/debug"
	log =			"outputs157/log.txt"
	output =		"outputs157/models"
	statistics =		"outputs157/stats.csv" 
}
