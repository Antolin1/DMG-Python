import epackage	"inputs/rdsSimplified.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package rdsSimplified }
	constraints = { package queries }
	solver = ViatraSolver
	scope = {
		#node += 15,
		#<rdsSimplified::Database> += 1..1
	}
	
	config = {
		runtime = 180,
		log-level = normal
	}
	
	runs = 1
	number = 1
	
	debug =			"outputs570/debug"
	log =			"outputs570/log.txt"
	output =		"outputs570/models"
	statistics =		"outputs570/stats.csv" 
}
