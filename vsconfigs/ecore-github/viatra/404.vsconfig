import epackage	"inputs/smallEcore.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package smallEcore }
	constraints = { package queries }
	solver = ViatraSolver
	scope = {
		#node += 17,
		#<smallEcore::EPackage> += 1..1
	}
	
	config = {
		runtime = 180,
		log-level = normal
	}
	
	runs = 1
	number = 1
	
	debug =			"outputs404/debug"
	log =			"outputs404/log.txt"
	output =		"outputs404/models"
	statistics =		"outputs404/stats.csv" 
}
