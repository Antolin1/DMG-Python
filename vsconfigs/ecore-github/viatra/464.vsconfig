import epackage	"inputs/smallEcore.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package smallEcore }
	constraints = { package queries }
	solver = ViatraSolver
	scope = {
		#node += 10,
		#<smallEcore::EPackage> += 1..1
	}
	
	config = {
		runtime = 180,
		log-level = normal
	}
	
	runs = 1
	number = 1
	
	debug =			"outputs464/debug"
	log =			"outputs464/log.txt"
	output =		"outputs464/models"
	statistics =		"outputs464/stats.csv" 
}
