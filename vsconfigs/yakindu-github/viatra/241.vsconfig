import epackage	"inputs/yakindu_simplified.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package yakindumm }
	constraints = { package queries }
	solver = ViatraSolver
	scope = {
		#node += 15,
		#<yakindumm::Statechart> += 1..1
	}
	
	config = {
		runtime = 180,
		log-level = normal
	}
	
	runs = 1
	number = 1
	
	debug =			"outputs241/debug"
	log =			"outputs241/log.txt"
	output =		"outputs241/models"
	statistics =		"outputs241/stats.csv" 
}
