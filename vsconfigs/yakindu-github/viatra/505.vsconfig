import epackage	"inputs/yakindu_simplified.ecore"
import viatra	"src/queries/constraints.vql"

generate {
	metamodel = { package yakindumm }
	constraints = { package queries }
	solver = ViatraSolver
	scope = {
		#node += 13,
		#<yakindumm::Statechart> += 1..1
	}
	
	config = {
		runtime = 180,
		log-level = normal
	}
	
	runs = 1
	number = 1
	
	debug =			"outputs505/debug"
	log =			"outputs505/log.txt"
	output =		"outputs505/models"
	statistics =		"outputs505/stats.csv" 
}
